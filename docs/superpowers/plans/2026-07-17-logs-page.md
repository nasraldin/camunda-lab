# Logs page enhancement — Implementation Plan

> **For agentic workers:** Implement task-by-task. No backend changes. Do not commit unless the user asks.

**Goal:** Enhance Lab UI Logs with Services-style controls and Filter/Highlight search over the streamed buffer.

**Architecture:** Client-only. Shared `serviceNames.ts`. EventSource streaming unchanged. Search filters or highlights the in-memory line buffer.

**Tech Stack:** React + TypeScript (Vite), existing Lab CSS variables.

## Global Constraints

- Friendly service names must match Services page
- Filter mode is default; Highlight is alternate
- Max buffer remains ~2000 lines
- No regex / no API changes

---

### Task 1: Shared service names

**Files:**
- Create: `internal/ui/web/src/serviceNames.ts`
- Modify: `internal/ui/web/src/pages/Containers.tsx` — import `friendlyName`

- [ ] Export `FRIENDLY` map and `friendlyName(service: string): string`
- [ ] Remove local map from Containers; import from `serviceNames`

### Task 2: Logs page + styles

**Files:**
- Modify: `internal/ui/web/src/pages/Logs.tsx`
- Modify: `internal/ui/web/src/styles.css`

- [ ] Page head with link to Services
- [ ] Friendly service select; stop+clear on service change
- [ ] Search input + Filter/Highlight chips + meta line + live pill
- [ ] Filter / highlight rendering; near-bottom auto-scroll when following
- [ ] CSS for `.logs-toolbar`, `.log-meta`, `mark.log-hit`, taller log viewer

### Task 3: Build and verify

- [ ] `npm run build` + `go build` + restart UI on 9091
- [ ] Manual: load recent logs, search filter, switch highlight, follow, View logs from Services
