{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "schemaVersion": 39,
  "tags": ["camunda-lab", "connectors"],
  "title": "Camunda Lab — Connectors",
  "uid": "camunda-lab-connectors",
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "",
  "templating": { "list": [] },
  "panels": [
    {
      "type": "stat",
      "title": "Connectors up",
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "datasource": null,
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "targets": [
        { "expr": "sum(up{job=\"connectors\"})", "legendFormat": "up" }
      ]
    },
    {
      "type": "timeseries",
      "title": "JVM heap used (bytes)",
      "gridPos": { "h": 8, "w": 12, "x": 6, "y": 0 },
      "datasource": null,
      "fieldConfig": { "defaults": { "unit": "bytes" }, "overrides": [] },
      "targets": [
        { "expr": "jvm_memory_used_bytes{job=\"connectors\"}", "legendFormat": "{{area}} {{id}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP server requests/s",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "datasource": null,
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "targets": [
        { "expr": "rate(http_server_requests_seconds_count{job=\"connectors\"}[1m])", "legendFormat": "{{method}} {{status}}" }
      ]
    },
    {
      "type": "timeseries",
      "title": "Live threads",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "datasource": null,
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "targets": [
        { "expr": "jvm_threads_live_threads{job=\"connectors\"}", "legendFormat": "threads" }
      ]
    }
  ]
}
